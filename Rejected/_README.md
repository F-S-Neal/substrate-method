# Rejected
Ideas you killed but kept. When you tag a spark `#rejected`, Auto Note Mover
files it here automatically. Browse-only — the `#rejected` tag is what actually
keeps these out of synthesis, not this folder.
